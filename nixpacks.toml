[phases.setup]
nixpkgs = ["flutter", "python310", "gcc"]

[phases.build]
cmds = [
    "flutter build web","pip install -r requirements.txt"]

[start]
cmd = "python signlanguage/python_model/inference_classifier.py"
